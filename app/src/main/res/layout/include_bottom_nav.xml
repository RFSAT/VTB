<?xml version="1.0" encoding="utf-8"?>
<!-- Toolbar row (v20.4): the BottomNavigationView holds all five tabs —
     Home, Capture, Results, Settings, Log — exactly at Material's 5-item
     cap (About's removal freed the slot Results now occupies). Exit stays
     appended outside the capped menu, visually matched to the items. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="?attr/colorSurface">

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        app:itemIconTint="@color/bottom_nav_item"
        app:itemTextColor="@color/bottom_nav_item"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu" />

    <LinearLayout
        android:id="@+id/btnExit"
        android:layout_width="56dp"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/nav_exit">

        <!-- Icon-only: contentDescription on the parent keeps it
             accessible for TalkBack without a visible label. -->
        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_nav_exit"
            app:tint="?android:attr/textColorPrimary"
            android:importantForAccessibility="no" />
    </LinearLayout>
</LinearLayout>
